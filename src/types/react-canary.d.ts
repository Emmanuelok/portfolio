/// <reference types="react/canary" />

// Activates the React canary type surface (ViewTransition and friends)
// project-wide. The App Router ships the canary runtime, so these APIs
// exist at runtime; this reference only teaches TypeScript about them.
